source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in panda_cms.gemspec.
gemspec

# Pending merge of https://github.com/ViewComponent/view_component/pull/1951 which adds engine support
gem "view_component", github: "viewcomponent/view_component", branch: "main"
