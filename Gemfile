source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in panda_cms.gemspec.
gemspec

gem "panda_core" # , path: "../panda_core"
